# Ensure platform-agnostic dependencies
optional=false
# Use the latest npm
engine-strict=true
# Disable package-lock.json as we're using pnpm
package-lock=false
# Ensure consistent dependency resolution
legacy-peer-deps=true
